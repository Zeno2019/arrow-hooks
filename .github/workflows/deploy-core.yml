name: Deploy Core to npm

on:
  push:
    branches: [master]
    paths:
      - "core/package.json"

jobs:
  check-version:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      version-changed: ${{ steps.check.outputs.changed }}
      new-version: ${{ steps.check.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check if version changed
        id: check
        run: |
          current_version=$(node -p "require('./core/package.json').version")

          git checkout HEAD~1 -- core/package.json 2>/dev/null || echo "First commit"
          previous_version=$(node -p "require('./core/package.json').version" 2>/dev/null || echo "0.0.0")
          git checkout HEAD -- core/package.json

          echo "Previous version: $previous_version"
          echo "Current version: $current_version"

          if [ "$current_version" != "$previous_version" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "version=$current_version" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

  publish:
    needs: check-version
    if: needs.check-version.outputs.version-changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "pnpm"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: pnpm install

      - name: Type Check
        run: cd core && pnpm run type-check --if-present

      - name: Run Tests
        run: cd core && pnpm run test --if-present

      - name: Build
        run: cd core && pnpm run build --if-present

      - name: Publish to npm
        run: cd core && npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create Git Tag
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git tag v${{ needs.check-version.outputs.new-version }}
          git push origin v${{ needs.check-version.outputs.new-version }}
